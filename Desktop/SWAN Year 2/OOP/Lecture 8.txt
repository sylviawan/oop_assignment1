git Commands

git init — initialise

git remote add origin — 

git pull origin master — synchronising, pull the changes from the remote git repository 

remote repository to local repository 

//commit is a snapshot of your file system, everything stored in your repository
git add .

git commit -m “Message beside it”

git push

//You gotta merge a changing?

//Read first three chapters of the git manuel